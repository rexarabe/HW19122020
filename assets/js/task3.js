//Задания

/*
* 1. Вывести только высокостные годы из диапазона start - end
* 2. Вернуть первые
* */
/*function isLeapYear(year){
    let

    if (year %4  ==0 && year % 100 == 0){
        return year = true;
    }
    else  return year = false;
}
console.log(isLeapYear(2020));
*
 */

function myFunction4_1(arr){
    arr.reduce(function());
};
//Функция редьюсерд для суммы элементов массива
const reducer1 = (accumulator, currentValue) => accumulator + currentValue;
//Функция редьюсер для произведения элементов массива
const reducer2 = (accumulator, currentValue ) => accumulator * currentValue;
//сумма квадратов
const reducer3 = (accumulator, currentValue)